[pytest]
testpaths = tests
pythonpath = .
markers =
    optional_deps: tests that require optional model/segmentation dependency stacks
    gpu: tests that exercise CUDA when it is available
    slow: tests that are slower than the default unit suite
